# Để tránh lỗi
uv pip install "numpy==2.3.0" để tránh lỗi Numba [3].
uv pip install "onnx==1.18.0" để tránh lỗi float4_e2m1fn [1].

# sau khi sửa code
git add .
git commit -m "Cập nhật tính năng X"
git push